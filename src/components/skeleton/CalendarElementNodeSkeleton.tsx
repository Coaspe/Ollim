const CalendarElementNodeSkeleton = () => {
  return (
    <div
      style={{
        width: "80%",
        aspectRatio: "1/1",
      }}
      className={`text-sm border border-gray-400 shadow-sm rounded-md flex items-center justify-center bg-gray-400 animate-pulse`}
    />
  );
};

export default CalendarElementNodeSkeleton;
